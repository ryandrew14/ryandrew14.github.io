My personal website as of 2026 (old one got lost in the Squarespace acquisition of Google Domains). Generated
by a small custom static site generator written in Rust with `tera` for templating.

## Building

```sh
cargo run
```

or

```sh
cargo run -- serve
```

for a small http server on localhost:8000

